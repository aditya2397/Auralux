#include "defn.h"
#include <stdio.h>

int main() {
	/* initialisations
		choose level
		place and initialise suns
		start timer
	*/
	while(1) {
		
	}
	/* game results
		stop time
		display total taken for level completion
		update fastest times in records file
	*/
	return 0;
}
